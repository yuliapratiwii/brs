APP_NAME=Laravel
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://brs-mu.vercel.app

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

# Vercel filesystem read-only, jadi log diarahkan ke stderr (muncul di Vercel Function Logs)
LOG_CHANNEL=stderr
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=
DB_PORT=3306
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

# Jangan pakai driver "database" untuk session/cache/queue di Vercel kecuali
# tabel sessions/cache/jobs sudah dimigrasi di database eksternal kamu.
# Default paling aman untuk serverless:
SESSION_DRIVER=cookie
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

CACHE_STORE=array

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# ---------------------------------------------------------------------
# CATATAN PENTING:
# File .env ini TIDAK dipakai saat deploy ke Vercel (filesystem read-only
# dan .env sudah di-gitignore). Semua value di atas harus diisi ulang
# secara manual di Vercel Dashboard > Settings > Environment Variables,
# untuk environment "Production".
# ---------------------------------------------------------------------
